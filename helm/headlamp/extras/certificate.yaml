apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headlamp-tls
  namespace: lab-gateway
spec:
  secretName: headlamp-tls
  dnsNames:
    - headlamp.tekton-lab.test
  issuerRef:
    name: step-ca-acme
    kind: ClusterIssuer
