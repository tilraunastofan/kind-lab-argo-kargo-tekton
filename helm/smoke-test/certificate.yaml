apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: smoke-tls
  namespace: lab-gateway
spec:
  secretName: smoke-tls
  dnsNames:
    - smoke.tekton-lab.test
  issuerRef:
    name: step-ca-acme
    kind: ClusterIssuer
